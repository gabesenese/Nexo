node_modules
**/node_modules
**/dist
.git
.github
mockups
*.md
# The unprefixed forms only match the context root, which left
# packages/server/.env (live provider keys and JWT_SECRET) being uploaded to
# the builder and baked into a layer.
**/.env
**/.env.*
!**/.env.example
